<Window x:Class="FileTransferServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Transfer Server" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#0078D4" CornerRadius="8" Padding="15" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="StatusText" Text="Starting..." FontSize="18" FontWeight="Bold" Foreground="White"/>
                    <TextBlock x:Name="IpText" Text="IP: " FontSize="13" Foreground="#CCFFFFFF" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <TextBlock x:Name="CountText" Text="0 files" FontSize="16" Foreground="White" HorizontalAlignment="Right"/>
                    <Button x:Name="ToggleBtn" Content="Stop Server" Click="ToggleServer" Width="120" Height="30" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <ProgressBar x:Name="MainProgress" Grid.Row="1" Height="6" Margin="0,0,0,10" Visibility="Collapsed" Foreground="#0078D4"/>

        <ListView Grid.Row="2" x:Name="FileList" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="File" Width="250" DisplayMemberBinding="{Binding FileName}"/>
                    <GridViewColumn Header="Size" Width="100" DisplayMemberBinding="{Binding Size}"/>
                    <GridViewColumn Header="Progress" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ProgressBar Value="{Binding Progress}" Height="16" Minimum="0" Maximum="100" Margin="2"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Status" Width="120" DisplayMemberBinding="{Binding Status}"/>
                    <GridViewColumn Header="Time" Width="140" DisplayMemberBinding="{Binding Time}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StatusBar Grid.Row="3" Background="#F5F5F5">
            <StatusBarItem>
                <TextBlock x:Name="FooterText" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
